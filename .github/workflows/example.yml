on:
  - push
  - pull_request
name: example
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - run: aws sts get-caller-identity && aws s3 ls

      - run: env | sort
